chai = require 'chai'
expect = chai.expect
mockery = require 'mockery'
should = chai.should()
sinon = require 'sinon'

global.expect = expect
global.mockery = mockery
global.should = should
global.sinon = sinon
